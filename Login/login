<!DOCTYPE html>
<html>
<head>
<title>LOGIN PAGE</title>
    <link rel="stylesheet" type="text/css">
    <style>
        body{
            margin: 0;
            padding: 0;
            background: url("image file name");
            background-size: auto;
            background-position: center;
            font-family: sans-serif;
        }
        .loginbox{
            width: 320px;
            height: 460px;
            background: rgba(0, 0, 0, 0.5);
            color:  #ffffff;
            top: 50%;
            left: 50%;
            position: absolute;
            transform: translate(-50%,-50%);
            box-sizing: border-box;
            padding: 70px 30px;
        }
        h1{
            margin: 0;
            padding: 0 0 20px;
            text-align: center;
            font-size: 22px;
        }6
        .loginbox input{
            width: 100%;
            margin-bottom: 20px;
        }
         .loginbox input[type="text"], input[type="password"]{
            border: none;
            border-bottom: 1px solid #fff;
            background: transparent;
            outline: none;
            height: 40px; 
            color: #ffffff;
            font-size: 16px;
        }
        .loginbox input[type="submit"]{
            border: none;
            outline: none;
            height: 40px;
            background: #2c9bef;
            color: #fff;
            font-size: 18px;
            border-radius: 20px;
        }
        .loginbox input[value="signup"]{
            border: none;
            outline: none;
            height: 30px;
            width: 70px;
            background: #aa9bef;
            color: #fff;
            font-size: 14px;
            border-radius: 20px;
        }
    </style>
    </head>
<body>
    <div class="loginbox"><h1>LOGIN</h1>
    <form action="loginphp.php" method="post">
        <label>    UserName : <input type="text" name="usname"><br><br></label>
        <label>    Password : <br><input type="password" name="pass1"><br><br></label>
                                  <input type="submit" value="login">
        </form>
        <form action="signup.php"> 
        <label> New to this site : <input type="submit" value="Signup"></label>
            </form>
    </div>
    </body>
</html>
